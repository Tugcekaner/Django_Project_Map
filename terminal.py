
# ! Ilk kurulum aşamaları

# * pip = package installer python
# ? pip --version (python versiyonunu öğrenme)
# * pip install django (django yükleme)
# ? pip list (django kontrolu için)
# * pip freeze > requirements.txt
# ? python -m pip install --upgrade pip (python güncelleme)

# ! Proje komutları

# * django-admin startproject yeniProje (proje oluşturma)
# ? py manage.py startapp myApp (proje içine app oluşturma)
# * py manage.py runserver (projeyi çalıştırma)
# ? py manage.py migrate (veri tabanını güncelleme)
# * py manage.py makemigrations (modeli oluşturmak için)
# ? py manage.py createsuperuser (admin oluşturma)

# ! Proje içi komutlar

# * install pillow (imagefield kullanabilmek için gerekli)
